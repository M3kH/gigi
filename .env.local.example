# Gigi Local Development Environment (AIO)
# Copy to .env.local and customize:
#   cp .env.local.example .env.local
#
# ── Dev workflow (recommended) ────────────────────────────────
#
#   ./dev.sh          # Start AIO infra + dev servers with HMR
#   ./dev.sh --fresh  # Wipe state and start fresh
#
#   Open http://localhost:5173
#
# ── Prod-like workflow (full Docker build, no HMR) ──────────
#
#   docker-compose -f docker-compose.local.yml --env-file .env.local --profile prod up --build
#
#   Open http://localhost:3100
#

# Gitea admin user (created automatically)
ADMIN_USER=admin
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=admin

# Gitea organization name
ORG_NAME=acme

# Path to local bare/working repos to import into Gitea
# Each subdirectory becomes a repo under the org
# REPOSITORIES_PATH=/path/to/your/repos

# Telegram bot token (optional — get from @BotFather)
# TELEGRAM_TOKEN=

# Claude OAuth token (optional — run "claude setup-token" to get yours)
# CLAUDE_TOKEN=sk-ant-...
