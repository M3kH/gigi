{
  "mcpServers": {
    "gigi-tools": {
      "type": "stdio",
      "command": "node",
      "args": ["--import", "tsx", "lib/core/mcp.ts"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    },
    "chrome-devtools": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest", "--browserUrl", "${CHROME_CDP_URL}", "--no-usage-statistics"]
    }
  }
}
