{
  "mcpServers": {
    "gigi-tools": {
      "type": "stdio",
      "command": "node",
      "args": ["--import", "tsx", "lib/core/mcp.ts"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
